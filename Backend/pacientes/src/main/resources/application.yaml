spring:
  application:
    name: pacientes
  datasource:
    url: jdbc:postgresql://localhost:5432/healthsyspacientes
    username: wadson
    password: postgres

server:
  port: 8082

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs